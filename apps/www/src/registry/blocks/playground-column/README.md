# Playground Column

A self-contained chat testing column with per-column model selection and controls.

## Usage

```tsx
import PlaygroundColumn from "~/registry/blocks/playground-column/playground-column";

export default function Example() {
  return <PlaygroundColumn />;
}
```

This block mounts a minimal provider for the preview and is safe to import in any project.
